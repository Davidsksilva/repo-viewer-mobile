import React from 'react';
import { View, Text } from 'react-native';

const Main = () => {
  return (
    <View>
      <Text />
    </View>
  );
};

Main.navigationOptions = {
  title: 'Users',
};

export default Main;
